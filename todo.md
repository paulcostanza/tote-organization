## Search 
- add x on right side for a quick delete of text
- Add regex for...
    - "metal/silicone tongs" -> metal tongs | silicone tongs
    - & -> and
    - and -> &
    - digit to spelling of number
    - spelling of number to digit
- add a magnifying search button
    - search bar is hidden until user selects search button
    - search hides arrow buttons (not yet implemented)

## Style...
- needs some color
- need to make styling dynamic in JS to prevent border-radius issues -> look at last list item in a div section

## Random
- how do web developers test everything with different web browsers before pushing?
- add arrow buttons to jump from tote to tote
    - only on screen when user is not searching

## Bugs
- Too many words in title will push number down and out of the row, and will affect the next line's quantity. You can still tell what it is, but it looks bad. However, if the word gets pushed down so the title takes up two lines then the number looks good (wtf)